# Lara-Todo

A sample ToDo activity list project built using the Laravel framework.


### Get started

``` 
$ git clone
$ cp .env.example .env
$ composer install
$ php artisan migrate
$ php artisan db:seed
$ php artisan serve
```
